## Visual Studio / .NET
.vs/
.claude/settings.local.json
bin/
obj/
*.user
*.suo
*.cache
*.log

## NuGet
packages/
*.nupkg

## Build results
[Dd]ebug/
[Rr]elease/
x64/
x86/

## Local scratch files
DevMind_Builder.md
DevMind_CodeReview.md
DevMind_Template.md
CodeReview.md
CodeReviewBenchmarkWithBugs/

# DevMind trace output
.dm-trace/

# DevMind local memory store (agent-written, machine-local)
.devmind/

# DevMind agent scratch/report output accidentally written into the working tree
dm_out*.txt
dm_output.txt

# Self-contained publish staging + any loose executables (never commit binaries)
.publish-*/
*.exe

# Compiled output — never commit binaries
publish/

# dotnet test output
TestResults/

# dist build/runtime output — keep ONLY the launchers + deploy script, ignore the rest
dist/*
!dist/devmind.cmd
!dist/dm.cmd
!dist/deploy-devmind.ps1
!dist/dm-watch.cmd
!dist/dm-watch.ps1

# Claude extended memory (local only)
.claude-memory.md

deploy-log.txt

# DevMind's own per-repo memory - written into the working tree at runtime
MEMORY.md
.devmind/memory/

# Local scratch/debug leftovers - never part of a build
_scratch_*
reddit-draft.md
